import { TestBed ,async} from "@angular/core/testing";
import { By } from "@angular/platform-browser";
import { RouterOutlet } from "@angular/router";

import { RouterTestingModule } from "@angular/router/testing";

import { AuthComponent } from "./auth.component";


describe("Verificacion de outlet",()=>{
   /* beforeEach(async(() => {
        TestBed.configureTestingModule({
          declarations: [
            AuthComponent
          ],imports:[RouterTestingModule.withRoutes([])]
        }).compileComponents();
      }));   


    it("Se verifica que exista el enrutador outlet en auth",()=>{
        const fixure = TestBed.createComponent(AuthComponent);
        let debugelement = fixure.debugElement.query(By.directive(RouterOutlet));

        expect(debugelement).not.toBeNull();
    });*/
    

})