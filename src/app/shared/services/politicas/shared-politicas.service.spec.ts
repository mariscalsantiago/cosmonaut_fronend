import { TestBed } from '@angular/core/testing';

import { SharedPoliticasService } from './shared-politicas.service';

describe('SharedPoliticasService', () => {
  // let service: SharedPoliticasService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(SharedPoliticasService);
  // });

  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
