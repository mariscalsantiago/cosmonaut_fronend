import { TestBed } from '@angular/core/testing';

import { SharedCompaniaService } from './shared-compania.service';

describe('SharedCompaniaService', () => {
  // let service: SharedCompaniaService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(SharedCompaniaService);
  // });

  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
