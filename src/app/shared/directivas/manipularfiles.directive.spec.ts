import { ManipularfilesDirective } from './manipularfiles.directive';

describe('ManipularfilesDirective', () => {
  it('should create an instance', () => {
    const directive = new ManipularfilesDirective();
    expect(directive).toBeTruthy();
  });
});
